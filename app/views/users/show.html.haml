%h3
  %span.bold= @user.login
  = link_to image_tag("feed-icon-28x28.png", :width => 20), user_path(params[:id], :atom)

- if current_user == @user
  %br
  %small
    = link_to "Editer mon profil", edit_user_path(@user)
    %br
    = link_to "Supprimer mon compte", @user, :confirm => 'Etes vous sûr?', :method => :delete

%br
%br

%h4
  Authored
%table
  %tr
    %th.small{:width => "50%", :align => :left} Name
    %th.small{:width => "50%", :align => :left} Type
    %th.small{:width => "50%", :align => :left} Author
  - @authored.each do |ins|
    %tr
      %td= link_to ins.name, ins
      %td= link_to ins.type, types_path(ins.type)
      %td= link_to ins.author, author_path(ins.author)
%h4
  Submissions
%table
  %tr
    %th.small{:width => "50%", :align => :left} Name
    %th.small{:width => "50%", :align => :left} Type
    %th.small{:width => "50%", :align => :left} Author
  - @submitted.each do |ins|
    %tr
      %td= link_to ins.name, ins
      %td= link_to ins.type, types_path(ins.type)
      %td= link_to ins.author, author_path(ins.author)